from addons_tests_register import VERSION
from setuptools import setup

# fmt: off

setup(
      name = "addons_tests_register"
    , version = VERSION
    , packages = ['addons_tests_register']
    )

# fmt: on
